Vim 7.0 (7.3+ recommended) or higher with Ruby or Python support required. 

We rely on a few plugins to make `vim` prettier and more functional. The first time you run `vim`, you'll need to install them by issuing the `:PlugInstall` command.

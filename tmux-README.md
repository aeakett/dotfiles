We use a few plugins to make `tmux` nicer to live in. Because of that, `tpm` needs to be installed. To do that:

```git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm```

The first time you run `tmux` you'll need to install the plugins: `^a I`

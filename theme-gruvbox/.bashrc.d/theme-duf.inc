# This is usually pointless since duf does a pretty good job of picking the right colours
# --theme={dark|light|ansi}
#
# also available for themes that respect really dumb terminals that don't understand unicode:
# --style={unicode|ascii}
alias duf='duf --theme=dark'

- `host-<HOSTNAME>` packages contain host-specific configuration details.
- Only stow one host at a time.
  - To change host packages, `stow -D` your current package first.
  - Forgot which host package you currently have stowed? Check `~/.config/.dotHost`.
